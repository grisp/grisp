version: 2

shared:
  build: &build
    steps:
      - checkout
      - run: echo "Test"

jobs:
  erlang-21:
    <<: *build
    docker:
      - image: erlang:21
  erlang-20:
    <<: *build
    docker:
      - image: erlang:20

workflows:
  version: 2
  build_and_test:
    jobs:
      - erlang-21
      - erlang-20
